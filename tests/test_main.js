// Example test code for your bot
const assert = require('assert');

describe('Telegram Bot', () => {
    it('should return event info on /start', () => {
        // Simulate /start command and check response
    });

    it('should register a user on /register', () => {
        // Simulate /register command and check response
    });
});
